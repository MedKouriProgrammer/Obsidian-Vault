	# I can call functions in main in one line if I the first is function , but if the firste is procedures i can't call theme in one line.

But Two procedures, it’s impossible to call them in one line of code.

If I want to call boolean in final function, I should write it with the word in one(~).